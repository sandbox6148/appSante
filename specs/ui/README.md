# specs/ui/

Maquettes et schémas d'écran, nommés `UC-###-nom-ecran.png` (ou `.html` pour un export d'outil de design), référencés depuis la section 9 « Interface » du UC concerné. Une maquette **illustre** la description structurelle du UC ; elle ne la remplace pas et n'est pas une source pour l'agent en cas de désaccord avec le texte.
